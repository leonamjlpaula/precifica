## 2026-03-02 - US-015
- What was implemented: Página de Detalhes do Procedimento (/procedimentos/[especialidade]/[id]) com resumo financeiro, edição de tempo, tabela de materiais editável e deleção de procedimentos customizados
- Files changed:
  - src/app/(dashboard)/procedimentos/[especialidade]/[id]/page.tsx (new — server component; fetches detail + all user materials, redirects if not found)
  - src/presentation/components/procedimentos/ProcedimentoDetailPage.tsx (new — client component with all interactions)
  - prd.json (US-015 marked passes: true)
- **Learnings for future iterations:**
  - Detail page pattern: server fetches both `getProcedimentoDetail` and `getMateriais` (for the add-material select), passes both to client component
  - After each mutation, call `router.refresh()` to re-fetch server data and recalculate prices — simpler than maintaining local computed state
  - `parseConsumoNumerico` is importable in client components (it's not a 'use server' file) — reuse it for client-side cost display
  - Delete flow: `deleteProcedimento` returns success → `router.push` to list page (not refresh); on error → close dialog + show toast
  - Inline editing pattern: track `editingPmaId` (null = none editing); render Input instead of text when id matches; Enter/Escape for keyboard UX
---

## 2026-03-02 - US-014
- What was implemented: Server actions para edição de Procedimentos (tempo e materiais)
- Files changed:
  - src/application/usecases/procedimentoActions.ts (updated — added 6 new server actions + ActionResult type)
  - prd.json (US-014 marked passes: true)
- **Learnings for future iterations:**
  - ProcedimentoMaterial ownership validation: use `include: { procedimento: true }` then check `pma.procedimento.userId === userId` — no need for separate lookup
  - addMaterialToProcedimento ordem: use `prisma.procedimentoMaterial.count({ where: { procedimentoId } })` then count+1 for next position
  - ActionResult type `{ success: boolean; error?: string }` exported from procedimentoActions.ts for reuse in UI components
  - deleteProcedimento guards against non-custom deletion with `isCustom` check before deleting — returns clear error message
---

## 2026-03-02 - US-013
- What was implemented: Página de Listagem de Procedimentos (/procedimentos/[especialidade]) com navegação por especialidades, tabela com cálculo de preço e comparativo VRPO
- Files changed:
  - src/app/(dashboard)/procedimentos/[especialidade]/page.tsx (new — server component; fetches all 11 especialidades with groupBy counts, resolves current especialidade, routes to /diagnostico if slug invalid, supports ?q= search param)
  - src/presentation/components/procedimentos/ProcedimentosPage.tsx (new — client component with: desktop sidebar nav with counts, mobile dropdown select, global search with router.push URL navigation, table with 7 columns, color-coded Diferença, Customizado badge, row click to detail page, Add dialog)
  - src/application/usecases/procedimentoActions.ts (updated — added createProcedimentoCustomizado server action + CreateProcedimentoResult type; handles P2002 unique constraint for duplicate code)
  - prd.json (US-013 marked passes: true)
- **Learnings for future iterations:**
  - Next.js 15 dynamic params: `params` and `searchParams` are Promises — always `await params` and `await searchParams` in server components
  - prisma.procedimento.groupBy({ by: ['especialidadeId'], where: { userId }, _count: { id: true } }) for efficient count-per-specialty in one query
  - URL-based search: server component reads `searchParams.q`, calls searchProcedimentos (all specialties) vs getProcedimentosByEspecialidade — cleaner than client-side filtering for cross-specialty search
  - P2002 unique constraint error: check `typeof e === 'object' && e !== null && 'code' in e && (e as { code: string }).code === 'P2002'` without needing Prisma import
  - router.refresh() after createProcedimentoCustomizado to re-fetch server data with the new procedure
---

## 2026-03-02 - US-012
- What was implemented: Interface, repositório e server actions para listagem e cálculo de preço de Procedimentos
- Files changed:
  - src/application/interfaces/IProcedimentoRepository.ts (new — interface with listByUserAndEspecialidade, getDetail; exports ProcedimentoWithMateriais type)
  - src/infrastructure/repositories/PrismaProcedimentoRepository.ts (new — Prisma implementation; includes especialidade + materiais with material, ordered by codigo/ordem)
  - src/application/usecases/calcularPrecoProcedimento.ts (new — parseConsumoNumerico helper + calcularPrecoProcedimento function returning { custoVariavel, custoFixoTotal, precoFinal })
  - src/application/usecases/procedimentoActions.ts (new — 'use server' file with getProcedimentosByEspecialidade and searchProcedimentos)
  - prd.json (US-012 marked passes: true)
- **Learnings for future iterations:**
  - consumo field is a string like "1 par", "2 tubetes", "0,5ml", "cobertura" — use parseConsumoNumerico to extract numeric value (handles Brazilian comma decimals, defaults to 1 if no leading number)
  - VRPOReferencia Prisma accessor: `prisma.vRPOReferencia` (first letter lowercased from `VRPOReferencia`)
  - Especialidade lookup: server action resolves slug (e.g. 'diagnostico') → especialidade.id via prisma.especialidade.findUnique({ where: { codigo: slug } })
  - Batch VRPO ref lookup: collect all codes from the procedures list, then prisma.vRPOReferencia.findMany({ where: { codigo: { in: codigos } } }), build a Map for O(1) lookup
  - calcularPrecoProcedimento is a non-'use server' pure function — importable by both server and client code for real-time UI calculation
  - searchProcedimentos uses mode: 'insensitive' for case-insensitive search — requires PostgreSQL full-text or simple index for perf at scale
---

## 2026-03-02 - US-011
- What was implemented: Página de Materiais (/materiais) com tabela, busca, edição inline de preço, modal adicionar, exclusão com validação
- Files changed:
  - src/app/(dashboard)/materiais/page.tsx (new — server component, fetches materials, passes to client)
  - src/presentation/components/materiais/MateriaisTable.tsx (new — client component with all features)
  - src/presentation/components/ui/badge.tsx (new — Badge UI component, shadcn/ui style)
  - src/presentation/components/ui/dialog.tsx (new — Dialog UI component using @radix-ui/react-dialog)
  - prd.json (US-011 marked passes: true)
- **Learnings for future iterations:**
  - @radix-ui/react-dialog is already in package.json — use it for modals (Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogClose)
  - Badge component lives at src/presentation/components/ui/badge.tsx — use variant="secondary" for neutral badges, variant="outline" for custom
  - Inline edit pattern: track editingId + editingPreco in state; render input row when id matches; Enter/Escape keyboard shortcuts improve UX
  - For delete with conflict, deleteMaterial returns errors.procedimentos array — join with ', ' for toast description
  - Dialog open/close: use onOpenChange callback to reset form state when dialog closes
---

## 2026-03-02 - US-010
- What was implemented: Interface, repositório e server actions para CRUD de Materiais
- Files changed:
  - src/application/interfaces/IMaterialRepository.ts (new — interface with listByUserId, updatePrice, create, delete)
  - src/infrastructure/repositories/PrismaMaterialRepository.ts (new — Prisma implementation; uses where: { id, userId } compound filter for ownership)
  - src/application/usecases/materialActions.ts (new — 'use server' file with getMateriais, updateMaterialPrice, createMaterial, deleteMaterial)
  - prd.json (US-010 marked passes: true)
- **Learnings for future iterations:**
  - deleteMaterial checks ProcedimentoMaterial before deleting; returns list of procedure names on conflict so UI can show specific error
  - Repository uses Prisma compound where: { id, userId } to enforce ownership without extra lookup query
  - createMaterial always sets isDefault: false — only seed data has isDefault: true
  - Export CreateMaterialState.material (the created material) so the UI can optimistically add it without a refetch
---

## Codebase Patterns
- Toast: Toaster component must be in root layout.tsx (not dashboard layout) — use `useToast` hook + `toast({ title, description, variant })` in client components
- Page split pattern: server component (page.tsx) fetches data + gets session → passes initialData as props to a client component for interactivity
- Real-time computed values: mirror server-side domain logic (value object formulas) client-side in a plain function + useMemo — no need to import domain classes in client components
- All-at-once save: manage items in local state, pass full array to upsert action on save (repository does delete+recreate — no need for individual add/delete server actions)

- Procedimento price formula: custoVariavel = Σ (material.preco / pma.divisor) × parseConsumoNumerico(pma.consumo); precoFinal = (tempoMinutos × custoFixoPorMinuto) + custoVariavel
- parseConsumoNumerico: replace ',' with '.', match leading number, default to 1 (handles "1 par", "0,5ml", "cobertura")
- VRPOReferencia Prisma accessor is `prisma.vRPOReferencia` (V is lowercased)
- Especialidade lookup by slug: prisma.especialidade.findUnique({ where: { codigo: slug } }) — slug is the `codigo` field (e.g. 'diagnostico')

- Dashboard layout: server component (session check + redirect) wraps a client component (DashboardLayout) that handles nav, sidebar, and mobile bottom nav
- Active nav state: use `'/' + href.split('/')[1]` vs `'/' + pathname.split('/')[1]` to match base segment (handles nested routes like /procedimentos/diagnostico)
- Mobile nav "Mais" pattern: fixed bottom overlay grid with remaining nav items; close on backdrop click or item click
- getServerSession(authOptions) in server components for session access; signOut({ callbackUrl: '/login' }) in client components for logout
- useActionState from React 19 (not useFormState) for server action form state in client components
- Auth pages go in src/app/(auth)/ route group
- Server actions go in src/application/usecases/ as 'use server' files
- On successful form submission, return { success: true } and use useEffect + router.push() for client-side redirect (server-side redirect() doesn't work well with useActionState)
- Export both the action function AND the state type from server action files so client components can type the initialState


- next-auth@4 stable: authOptions in src/lib/auth.ts, API route at app/api/auth/[...nextauth]/route.ts, session JWT strategy
- next-auth type augmentation in src/types/next-auth.d.ts (extends Session.user with id: string, JWT with userId)
- Middleware at src/middleware.ts uses withAuth from next-auth/middleware — protect /dashboard/:path*
- session.user.id is populated via jwt callback (token.userId) → session callback (session.user.id = token.userId)
- Next.js 15.5.12 with App Router, TypeScript strict mode
- shadcn/ui configured manually via components.json (create-next-app won't work in existing directory with files)
- UI components live in src/presentation/components/ui/ (aliased as @/presentation/components/ui)
- All utilities in src/lib/utils.ts (cn helper)
- Hooks in src/presentation/hooks/
- Prisma schema at prisma/schema.prisma with postgresql provider
- Domain structure: src/domain/entities, src/domain/value-objects, src/application/usecases, src/application/interfaces, src/infrastructure/repositories, src/infrastructure/services
- Run typecheck with: npx tsc --noEmit
- next-auth@4 stable used (not v5 beta)
- bcrypt for password hashing (@types/bcrypt in devDependencies)
- No local PostgreSQL available — use `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` to generate migration SQL; run `npx prisma migrate dev` when DB is available
- Migration files committed to prisma/migrations/ so they're ready for deployment
- Prisma singleton at src/lib/db.ts — import `prisma` from '@/lib/db' in server code
- VRPO seed data (materials, procedures, createDefaultDataForUser) in src/lib/vrpo-seed-data.ts
- Global seed (Especialidades + VRPOReferencias) in prisma/seed.ts — run `npx prisma db seed` when DB ready
- seed.ts uses its own PrismaClient directly (not the singleton from db.ts) to avoid module resolution issues in ts-node
- prisma/seed.ts does NOT import from src/lib/* to avoid @/ alias resolution issues in ts-node CommonJS mode

- Repository pattern: interfaces in src/application/interfaces/, implementations in src/infrastructure/repositories/
- Value objects in src/domain/value-objects/ with static calculation methods (no DB calls inside)
- Use cases in src/application/usecases/ — non-server-action use cases are plain async functions (no 'use server')
- Server actions grouped by domain in single 'use server' file (e.g., custoFixoActions.ts with getCustoFixo, saveCustoFixo, addItem, deleteItem)

---

## 2026-03-02 - US-009
- What was implemented: Página de Custos Fixos (/custos-fixos) com banner, fórmula, 4 seções editáveis, add/delete itens customizados, toast on save
- Files changed:
  - src/app/(dashboard)/custos-fixos/page.tsx (new — server component fetches config, passes to CustosFixosForm)
  - src/presentation/components/custos-fixos/CustosFixosForm.tsx (new — client component with all form logic, real-time calculation, local item state)
  - src/app/layout.tsx (updated — added Toaster component for toast notifications)
  - prd.json (US-009 marked passes: true)
- **Learnings for future iterations:**
  - Pages follow server+client split: server component gets session+data, client component handles state/interactions
  - Real-time formula calculation: replicate value object math in a plain function + useMemo in client component (don't import server domain classes)
  - Item management: keep in local state, save all at once via upsert action — simpler than individual add/delete DB calls during editing
  - Toaster must be in root layout.tsx (not dashboard layout) to work across all pages
  - `useTransition` + `startTransition(async () => {...})` for server action calls with pending state
---

## 2026-03-02 - US-008
- What was implemented: Server actions e lógica de domínio para Custos Fixos
- Files changed:
  - src/application/interfaces/ICustoFixoRepository.ts (new — interface with getByUserId and upsert)
  - src/infrastructure/repositories/PrismaCustoFixoRepository.ts (new — Prisma implementation; upsert uses delete+recreate strategy for items)
  - src/domain/value-objects/CustoFixoPorMinuto.ts (new — static calculate(config, items) method)
  - src/application/usecases/calcularCustoFixoPorMinuto.ts (new — use case fetching config from DB and computing value)
  - src/application/usecases/custoFixoActions.ts (new — 'use server' file with getCustoFixoConfig, saveCustoFixoConfig, addCustoFixoItem, deleteCustoFixoItem)
  - prd.json (US-008 marked passes: true)
- **Learnings for future iterations:**
  - CustoFixoPorMinuto formula: custoFixoBase (totalItens/minutosUteis) + depreciacao (investimento/(anos*12*minutosUteis)) + remuneracao (salario*(1+%s)/minutosUteis) + taxaRetorno (investimento*taxa%/(anosRetorno*12*minutosUteis))
  - Repository upsert strategy: delete all items then recreate — simpler than tracking individual item changes by ID
  - Non-'use server' use cases (like calcularCustoFixoPorMinuto) are plain async functions — only files directly called from client components/Server Components need 'use server'
  - Server actions in custoFixoActions.ts accept userId as a parameter (caller must pass session.user.id) — no session fetching inside actions
  - Export result types from server action files so pages can type their state properly
---

## 2026-03-02 - US-007
- What was implemented: Layout da área autenticada com sidebar desktop e navegação mobile
- Files changed:
  - src/presentation/components/layout/DashboardLayout.tsx (new — client component with sidebar, header, mobile bottom nav, active state, logout)
  - src/app/(dashboard)/layout.tsx (new — server component with getServerSession, redirect to /login if no session, renders DashboardLayout)
  - src/app/(dashboard)/dashboard/page.tsx (new — placeholder dashboard page for US-017 to expand)
  - prd.json (US-007 marked passes: true)
- **Learnings for future iterations:**
  - Dashboard layout is split: server layout.tsx for session check + DashboardLayout client component for interactivity
  - Active nav detection: compare first path segments (`'/' + href.split('/')[1]` === `'/' + pathname.split('/')[1]`) to handle nested routes correctly
  - Mobile "Mais" button opens an overlay grid (fixed positioning, z-index above bottom nav) with backdrop close
  - Procedimentos sidebar link points to `/procedimentos/diagnostico` (first especialidade) as default destination
  - Use `shrink-0` on sidebar to prevent it from collapsing; use `min-w-0` on content area to prevent overflow
  - Bottom nav height: ~56px — add `pb-20` (80px) to main content on mobile to prevent content from being hidden behind it
---

## 2026-03-02 - US-006
- What was implemented: Página de login (/login) com formulário de email e senha
- Files changed:
  - src/app/(auth)/login/page.tsx (new — client component with signIn('credentials'), inline error, success message, link to /cadastro, placeholder 'Esqueci minha senha')
  - prd.json (US-006 marked passes: true)
- **Learnings for future iterations:**
  - Login page uses `signIn('credentials', { email, password, redirect: false })` from 'next-auth/react' — check result.error for failures
  - Use `useTransition` + `startTransition(async () => { ... })` for async signIn call with loading state
  - Success message from /cadastro redirect passes via `?success=...` query param — read with `useSearchParams()`
  - After successful login, use `router.push('/dashboard')` for client-side redirect
  - `signIn` with `redirect: false` returns `{ error: string | null, status: number, ok: boolean, url: string | null }`
  - The "Esqueci minha senha" is a disabled Button variant="link" (non-functional placeholder per MVP spec)
---

## 2026-03-02 - US-003
- What was implemented: Script de seed com dados VRPO (materiais, procedimentos, referências)
- Files changed:
  - src/lib/db.ts (new — Prisma singleton for app use)
  - src/lib/vrpo-seed-data.ts (new — 14 custo fixo items, 30 materials, 40 procedures with materials, createDefaultDataForUser function)
  - prisma/seed.ts (new — seeds 11 Especialidades + 75 VRPOReferencias globally)
  - package.json (added `prisma.seed` field for `npx prisma db seed`)
  - prd.json (US-003 marked passes: true)
- **Learnings for future iterations:**
  - seed.ts must NOT import from src/lib/* because ts-node CommonJS mode can't resolve @/ path aliases
  - Keep prisma/seed.ts self-contained with its own PrismaClient instance
  - createDefaultDataForUser(userId) in src/lib/vrpo-seed-data.ts uses the global prisma singleton from ./db
  - Function depends on Especialidades already being seeded globally — call createDefaultDataForUser AFTER npx prisma db seed
  - DEFAULT_PROCEDIMENTOS references materials by nome (string lookup) — material names must match exactly between DEFAULT_MATERIAIS and procedure compositions
  - No local DB — typecheck passes but `npx prisma db seed` needs a real DB connection
---

## 2026-03-02 - US-001
- What was implemented: Setup do projeto Next.js com stack completa
- Files changed:
  - package.json (Next.js 15.5.12, Tailwind, Prisma, NextAuth, Zod, @react-pdf/renderer, xlsx, shadcn/ui deps)
  - tsconfig.json, tailwind.config.ts, postcss.config.mjs, eslint.config.mjs, next.config.ts
  - components.json (shadcn/ui config)
  - .env.example, .gitignore
  - prisma/schema.prisma (all models defined)
  - src/app/globals.css (Tailwind + CSS variables)
  - src/app/layout.tsx, src/app/page.tsx
  - src/lib/utils.ts (cn helper)
  - src/presentation/components/ui/ (button, card, input, label, toast, toaster)
  - src/presentation/hooks/use-toast.ts
  - Created folder structure: src/domain/entities, src/domain/value-objects, src/application/usecases, src/application/interfaces, src/infrastructure/repositories, src/infrastructure/services, src/presentation/components, src/presentation/hooks, src/lib
- **Learnings for future iterations:**
  - create-next-app@latest fails when directory has existing files — must create project manually
  - Prisma schema already has all models for the full PRD (US-002 schema is already done!)
  - shadcn/ui must be initialized manually via components.json + creating UI components directly
  - tailwindcss-animate must be installed separately as devDependency
  - Next.js 15.5.12 is the minimum secure version (15.1.6 has critical vulnerability CVE-2025-66478)
  - next-auth@4 stable works; use "next-auth": "^4.24.11"
  - The prisma/schema.prisma already covers US-002 acceptance criteria
---

## 2026-03-02 - US-002
- What was implemented: Schema Prisma com todos os modelos do banco de dados
- Files changed:
  - prisma/migrations/20260302000000_init/migration.sql (generated migration SQL via prisma migrate diff)
  - prisma/migrations/migration_lock.toml (Prisma migration lock file)
  - prd.json (US-002 marked as passes: true)
  - .env (created from .env.example — gitignored)
- **Learnings for future iterations:**
  - No local PostgreSQL available in this environment — `npx prisma migrate dev` fails with P1001
  - Use `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` to generate migration SQL without a DB connection
  - `npx prisma generate` works without a DB and generates the Prisma client for TypeScript type checking
  - `npx prisma validate` confirms schema syntax is valid without DB connection
  - Migration SQL files should be committed to version control so devs can apply them when they have a DB
  - When a real DB is available: run `npx prisma migrate dev` to apply migrations, then `npx prisma db seed` to populate data
---

## 2026-03-02 - US-004
- What was implemented: NextAuth configurado com provider Credentials (email/senha)
- Files changed:
  - src/lib/auth.ts (new — NextAuth authOptions with CredentialsProvider, JWT session, bcrypt password validation)
  - src/app/api/auth/[...nextauth]/route.ts (new — App Router handler exporting GET and POST)
  - src/middleware.ts (new — withAuth protecting /dashboard/:path*)
  - src/types/next-auth.d.ts (new — type augmentation for Session.user.id and JWT.userId)
- **Learnings for future iterations:**
  - next-auth v4 App Router API route: export handler as GET and POST (not default export)
  - next-auth type augmentation MUST be in a .d.ts file inside src/ so TypeScript picks it up
  - withAuth in middleware handles redirect to signIn page (configured in authOptions.pages.signIn)
  - session.user.id sourced from JWT token.userId set in jwt() callback when user object is present (login only)
  - NEXTAUTH_SECRET and NEXTAUTH_URL were already documented in .env.example from US-001
---

## 2026-03-02 - US-005
- What was implemented: Página de cadastro (/cadastro) com formulário de registro
- Files changed:
  - src/application/usecases/createUser.ts (new — server action with Zod validation, bcrypt, email uniqueness check, createDefaultDataForUser)
  - src/app/(auth)/cadastro/page.tsx (new — client component using useActionState, form with name/email/password/confirm, inline errors, success redirect)
  - prd.json (US-005 marked passes: true)
- **Learnings for future iterations:**
  - React 19 (used in Next.js 15) has `useActionState` stable — import from 'react', not 'react-dom'
  - `useActionState(action, initialState)` requires the initialState to match the return type of the action — use a typed const: `const initialState: ActionState = {}`
  - Route group `(auth)` in src/app/ — pages inside don't need auth, middleware already protects /dashboard only
  - App directory is at `src/app/` (not root `app/`) in this project
  - Server actions must be in separate files with 'use server' directive at the top
---
